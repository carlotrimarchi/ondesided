import { describe, it, expect } from "vitest";

describe("Setup", () => {
  it("should run tests", () => {
    expect(true).toBe(true);
  });

  it("can do math", () => {
    expect(1 + 1).toBe(2);
  });
});
